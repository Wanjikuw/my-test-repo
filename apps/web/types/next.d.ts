// Mirrors the auto-generated next-env.d.ts so `typecheck` passes before the first
// `next build` has run (CI typechecks before building). Next may also generate its
// own next-env.d.ts at the app root; duplicate reference directives are harmless.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
